=======================================================================

필수 제외 항목

=======================================================================

1. 환경 변수 파일 (비밀 정보 포함)

Supabase URL이나 SECRET_KEY 등 민감한 정보를 담고 있어 Git에 올리면 안 됩니다.

.env

2. Python 가상 환경 폴더

개발 환경마다 다르며, 'requirements.txt'를 통해 복구 가능합니다.

.venv/
venv/

3. Python 컴파일된 파일 및 캐시

성능 향상을 위해 파이썬이 생성하는 임시 파일입니다.

pycache/
*.pyc

Pydantic이 설정 파일을 로드할 때 생성할 수 있는 임시 파일

*pyc

4. IntelliJ IDEA 관련 파일 (IDE 설정)

사용자별 IDE 설정이므로, 팀원들과 공유할 필요가 없습니다.

.idea/
*.iml

5. DB 파일 (SQLite 등을 사용하는 경우)

이 프로젝트에서는 Supabase(클라우드 DB)를 사용하지만, 로컬 DB 파일도 제외합니다.

*.db
*.sqlite

6. 로그 및 빌드 출력 파일 (추가 항목)

애플리케이션 실행 중 생성되는 로그 파일 및 빌드 출력 파일입니다.

*.log
/dist/